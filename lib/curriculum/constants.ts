/**
 * Version string stored in JSON (`curriculumVersion`) and in `signup_curricula.curriculum_version`.
 * Bump when the curriculum payload shape changes in a breaking way.
 */
export const CURRICULUM_JSON_VERSION = "2.0.0" as const;
