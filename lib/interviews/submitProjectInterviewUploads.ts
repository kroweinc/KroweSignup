/** TODO: POST multipart to /api/interviews/upload (not built yet). */
export async function submitProjectInterviewUploads(
  projectId: string,
  files: File[]
): Promise<void> {
  void projectId;
  void files;
  await Promise.resolve();
  throw new Error("Interview file upload is not implemented yet.");
}
